apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-governance-core.fullname" . }}
  labels:
    {{- include "agent-governance-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-governance-core.selectorLabels" . | nindent 4 }}
