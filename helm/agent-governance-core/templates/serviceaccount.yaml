{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent-governance-core.serviceAccountName" . }}
  labels:
    {{- include "agent-governance-core.labels" . | nindent 4 }}
{{- end -}}
